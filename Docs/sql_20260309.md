## Phase 2 — 20条要求对照

| 要求 | 描述 | 修改内容 |
| :--- | :--- | :--- |
| **Req 2** | [cite_start]主外键referential integrity | tbl_Vehicles 里8个外键全部加了命名约束，格式 fk_Veh_Model、fk_Veh_Year 等；tbl_Listings 加了 fk_List_Vehicle、fk_List_Location、fk_List_Cond；tbl_Listing_Status 加了 fk_LStatus_Listing、fk_LStatus_Status [cite: 1, 2] |
| **Req 3** | 10个以上自定义CHECK约束 | [cite_start]原来只有4个，新增到14个：chk_Models_Name、chk_Years_Range、chk_Trims_Name、chk_Locations_Name、chk_Statuses_Label（限定4个合法值）、chk_Conditions_Label（限定5个合法值）、chk_Transmissions_Type（限定5个合法值）、chk_Drivetrains_Name、chk_Body_Styles_Name、chk_Colours_Name、chk_Veh_Title、chk_Veh_Hash（限定64位长度）、chk_List_Price（1到10,000,000）、chk_List_Kms（0到2,000,000）、chk_LStatus_Dates、chk_LStatus_ScrapeDate（不早于2020-01-01） [cite: 2] |
| **Req 4** | 优化数据类型 | [cite_start]低基数维度表主键改为 TINYINT（Statuses、Conditions、Transmissions、Drivetrains、Body_Styles、Seats）；年份、车型等改为 SMALLINT；价格从 DECIMAL(15,2) 改为 DECIMAL(10,2) [cite: 2] |
| **Req 12** | Staging表策略 | [cite_start]新增 tbl_stg_Raw，结构与原始CSV一致，含 Load_Timestamp 列，ETL先插入此表再分发到正式表 [cite: 2] |
| **Req 14** | Metadata追踪 | [cite_start]所有13张表都加了 CreatedDate DATETIME NOT NULL DEFAULT GETDATE()，插入时自动记录时间 [cite: 2] |
| **Req 15** | [cite_start]PII脱敏 | tbl_Vehicles 的 Link_URL 改为 Link_URL_Hash CHAR(64)，只存SHA-256哈希值；tbl_stg_Raw 同步改为 Link_URL_Hash [cite: 2] |
| **Req 17** | 索引优化（3个以上） | [cite_start]新增4个非聚集索引：idx_Listings_Price（Price_CAD）、idx_Listings_Kms（Kilometres）、idx_Locations_City（City_Name）、idx_LStatus_SoldDate（Sold_Date，带WHERE过滤只索引非NULL行） [cite: 2] |
| **Req 18** | Schema版本控制 | [cite_start]脚本开头加了 DROP DATABASE IF EXISTS + WAITFOR DELAY 逻辑，确保每次执行从干净状态重建 [cite: 2] |
| **Req 19** | 自动备份逻辑 | [cite_start]脚本末尾提供备份命令模板（注释状态），执行ETL前取消注释运行 [cite: 2] |
| **Req 20** | SQL命名规范 | [cite_start]所有表名加 tbl_ 前缀，主键约束改为 pk_，外键改为 fk_，CHECK约束改为 chk_，索引改为 idx_ [cite: 2] |

---

## Phase 4

### Req 10 技术数据字典
[cite_start]技术数据字典就是一份说明书，解释数据库里每一张表、每一列是什么意思、有什么规则。[cite: 3, 4, 5]

#### [cite_start]示例：tbl_Listings 的数据字典 [cite: 6]

| 列名 | 数据类型 | 允许NULL | 默认值 | 业务含义 | 约束规则 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| **Listing_ID** | INT | 否 | 无 | 每条挂牌记录的唯一ID | [cite_start]主键 [cite: 7] |
| **Vehicle_ID** | INT | 否 | 无 | 关联的车辆ID | [cite_start]外键 → tbl_Vehicles [cite: 7] |
| **Price_CAD** | DECIMAL(10,2) | 否 | 无 | 卖家标价（加元） | [cite_start]必须在1到10,000,000之间 [cite: 7] |
| **Kilometres** | INT | 否 | 无 | 车辆行驶里程 | [cite_start]必须在0到2,000,000之间 [cite: 7] |
| **Condition_ID** | TINYINT | 否 | 无 | 车况等级ID | [cite_start]外键 → tbl_Conditions [cite: 7] |
| **CreatedDate** | DATETIME | 否 | GETDATE() | ETL插入时间 | [cite_start]自动填入 [cite: 7] |
