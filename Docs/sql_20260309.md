Phase 2 — 20条要求对照
要求	描述	修改内容
Req 2	主外键referential integrity	tbl_Vehicles 里8个外键全部加了命名约束，格式 fk_Veh_Model、fk_Veh_Year 等；tbl_Listings 加了 fk_List_Vehicle、fk_List_Location、fk_List_Cond；tbl_Listing_Status 加了 fk_LStatus_Listing、fk_LStatus_Status
Req 3	10个以上自定义CHECK约束	原来只有4个，新增到14个：chk_Models_Name、chk_Years_Range、chk_Trims_Name、chk_Locations_Name、chk_Statuses_Label（限定4个合法值）、chk_Conditions_Label（限定5个合法值）、chk_Transmissions_Type（限定5个合法值）、chk_Drivetrains_Name、chk_Body_Styles_Name、chk_Colours_Name、chk_Veh_Title、chk_Veh_Hash（限定64位长度）、chk_List_Price（1到10,000,000）、chk_List_Kms（0到2,000,000）、chk_LStatus_Dates、chk_LStatus_ScrapeDate（不早于2020-01-01）
Req 4	优化数据类型	低基数维度表主键改为 TINYINT（Statuses、Conditions、Transmissions、Drivetrains、Body_Styles、Seats）；年份、车型等改为 SMALLINT；价格从 DECIMAL(15,2) 改为 DECIMAL(10,2)
Req 12	Staging表策略	新增 tbl_stg_Raw，结构与原始CSV一致，含 Load_Timestamp 列，ETL先插入此表再分发到正式表
Req 14	Metadata追踪	所有13张表都加了 CreatedDate DATETIME NOT NULL DEFAULT GETDATE()，插入时自动记录时间
Req 15	PII脱敏	tbl_Vehicles 的 Link_URL 改为 Link_URL_Hash CHAR(64)，只存SHA-256哈希值；tbl_stg_Raw 同步改为 Link_URL_Hash
Req 17	索引优化（3个以上）	新增4个非聚集索引：idx_Listings_Price（Price_CAD）、idx_Listings_Kms（Kilometres）、idx_Locations_City（City_Name）、idx_LStatus_SoldDate（Sold_Date，带WHERE过滤只索引非NULL行）
Req 18	Schema版本控制	脚本开头加了 DROP DATABASE IF EXISTS + WAITFOR DELAY 逻辑，确保每次执行从干净状态重建
Req 19	自动备份逻辑	脚本末尾提供备份命令模板（注释状态），执行ETL前取消注释运行
Req 20	SQL命名规范	所有表名加 tbl_ 前缀，主键约束改为 pk_，外键改为 fk_，CHECK约束改为 chk_，索引改为 idx_

Phase 4
Req 10技术数据字典
技术数据字典就是一份说明书，解释数据库里每一张表、每一列是什么意思、有什么规则。
________________________________________
举例，tbl_Listings 的数据字典：
列名	数据类型	允许NULL	默认值	业务含义	约束规则
Listing_ID	INT	否	无	每条挂牌记录的唯一ID	主键
Vehicle_ID	INT	否	无	关联的车辆ID	外键 → tbl_Vehicles
Price_CAD	DECIMAL(10,2)	否	无	卖家标价（加元）	必须在1到10,000,000之间
Kilometres	INT	否	无	车辆行驶里程	必须在0到2,000,000之间
Condition_ID	TINYINT	否	无	车况等级ID	外键 → tbl_Conditions
CreatedDate	DATETIME	否	GETDATE()	ETL插入时间	自动填入

